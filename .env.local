# ============================================================
# ENVIRONMENT VARIABLES CONFIGURATION
# ============================================================

# ========== BASE API CONFIGURATION ==========
# URL dasar untuk API backend
# Digunakan oleh frontend untuk semua request API
NEXT_PUBLIC_BASE_API='https://your-api-domain.com/'

# ========== AUTHENTICATION (NextAuth.js) ==========
# Secret key untuk enkripsi session dan token JWT
# Generate dengan: npx auth secret atau openssl rand -base64 32
# CRITICAL: Ganti di production dengan nilai unik!
AUTH_SECRET="your-generated-secret-key-here"

# ========== GOOGLE OAUTH CREDENTIALS ==========
# Client ID dan Secret dari Google Cloud Console
# Cara dapat: https://console.cloud.google.com/apis/credentials
# 1. Buat OAuth 2.0 Client ID
# 2. Set Authorized redirect URIs: http://localhost:3000/api/auth/callback/google
AUTH_GOOGLE_ID="your-google-client-id.apps.googleusercontent.com"
AUTH_GOOGLE_SECRET="GOCSPX-your-google-client-secret"

# ========== NEXTAUTH URL CONFIGURATION ==========
# Base URL aplikasi (untuk redirect OAuth)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# Trust host untuk NextAuth (same as NEXTAUTH_URL)
AUTH_TRUST_HOST=http://localhost:3000

# ========== IMAGE/MEDIA CONFIGURATION ==========
# URL publik untuk akses gambar/media yang diupload
# Digunakan untuk menampilkan gambar di frontend
NEXT_PUBLIC_API_IMAGE_URL=https://your-image-cdn.com/uploads

# ========== EMAIL SMTP CONFIGURATION ==========
# Konfigurasi untuk mengirim email (notifications, reset password, etc)
# Gmail SMTP settings (atau gunakan provider lain seperti SendGrid, Mailgun)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# Gmail account untuk mengirim email
# NOTE: Untuk Gmail, gunakan App Password (bukan password akun)
# Cara buat App Password: https://myaccount.google.com/apppasswords
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password-here

# Email admin untuk menerima notifikasi penting
ADMIN_EMAIL=admin@yourdomain.com

